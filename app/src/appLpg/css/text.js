import { StyleSheet } from 'react-native';

const stylesText = StyleSheet.create ({
    h1: { color: '#FF3030', fontSize: 32, fontWeight: 'bold' },
    p: { fontSize: 16 },
})

export default stylesText;